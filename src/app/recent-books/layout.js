export const metadata = {
  title: 'Recently Added Books - BookCafe',
  description: 'View the latest books added to your library catalog.',
  keywords: 'recent books, new additions, library catalog, book management',
};

export default function RecentBooksLayout({ children }) {
  return children;
} 