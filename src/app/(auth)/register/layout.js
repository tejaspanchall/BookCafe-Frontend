export const metadata = {
  title: 'Register',
  description: 'Create a new BookCafe account to access our digital library of educational resources.',
  keywords: 'register, sign up, create account, new user',
};

export default function RegisterLayout({ children }) {
  return children;
} 